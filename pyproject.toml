[project]
name = "shenas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dlt[duckdb]>=1.24.0",
    "rich>=14.3.3",
    "typer>=0.24.1",
]

[dependency-groups]
dev = [
    "commitizen>=4.13.9",
    "ruff>=0.15.8",
]

[tool.ruff]
line-length = 127

[tool.commitizen]
version_provider = "uv"
