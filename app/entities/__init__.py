"""Entity-graph tables living in the ``entities`` schema.

- :mod:`app.entities.properties` -- ``entities.properties``, the
  registry of predicates declared by Wikidata, source plugins, or the user.
- :mod:`app.entities.statements` -- ``entities.statements``, the
  (entity, property, value) triple store backing the entity graph.
"""
