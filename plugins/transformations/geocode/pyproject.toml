[project]
name = "shenas-transformation-geocode"
dynamic = ["version"]
description = "Geocode transformation plugin for shenas"
requires-python = ">=3.11"
dependencies = [
    "shenas-transformation-core",
    "geopy>=2.4.0",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project.entry-points."shenas.transformations"]
geocode = "shenas_transformations.geocode:GeocodeTransformation"

[tool.hatch.version]
source = "vcs"
tag-pattern = "transformation-geocode/v(?P<version>[^+]+)"
raw-options.root = "../../.."
raw-options.git_describe_command = ["git", "describe", "--dirty", "--tags", "--long", "--match", "transformation-geocode/v*"]

[tool.hatch.build.targets.wheel]
packages = ["shenas_transformations"]
