[project]
name = "shenas-transformation-dedup-merge"
dynamic = ["version"]
description = "Dedup/merge transformation plugin for shenas"
requires-python = ">=3.11"
dependencies = [
    "shenas-transformation-core",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project.entry-points."shenas.transformations"]
dedup-merge = "shenas_transformations.dedup_merge:DedupMergeTransform"

[tool.hatch.version]
source = "vcs"
tag-pattern = "transformation-dedup-merge/v(?P<version>[^+]+)"
raw-options.root = "../../.."
raw-options.git_describe_command = ["git", "describe", "--dirty", "--tags", "--long", "--match", "transformation-dedup-merge/v*"]

[tool.hatch.build.targets.wheel]
packages = ["shenas_transformations"]
