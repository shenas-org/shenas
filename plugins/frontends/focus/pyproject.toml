[project]
name = "shenas-frontend-focus"
dynamic = ["version"]
description = "Single-component view with bottom navigation and hotkey support"
requires-python = ">=3.11"
dependencies = [
    "shenas-frontend-core",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project.entry-points."shenas.frontends"]
focus = "shenas_frontends.focus:FocusUI"

[tool.hatch.version]
source = "vcs"
tag-pattern = "frontend-focus/v(?P<version>[^+]+)"
raw-options.root = "../../.."
raw-options.git_describe_command = ["git", "describe", "--dirty", "--tags", "--long", "--match", "frontend-focus/v*"]

[tool.hatch.build.targets.wheel]
packages = ["shenas_frontends"]
