[project]
name = "shenas-transformer-llm-categorize"
dynamic = ["version"]
description = "LLM categorization transformer plugin for shenas"
requires-python = ">=3.11"
dependencies = [
    "shenas-transformer-core",
    "shenas-app",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project.entry-points."shenas.transformers"]
llm-categorize = "shenas_transformers.llm_categorize.transform:LlmCategorizeTransformer"

[tool.hatch.version]
source = "vcs"
tag-pattern = "transformer-llm-categorize/v(?P<version>[^+]+)"
raw-options.root = "../../.."
raw-options.git_describe_command = ["git", "describe", "--dirty", "--tags", "--long", "--match", "transformer-llm-categorize/v*"]

[tool.hatch.build.targets.wheel]
packages = ["shenas_transformers"]
