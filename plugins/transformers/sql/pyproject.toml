[project]
name = "shenas-transformer-sql"
dynamic = ["version"]
description = "SQL transformer plugin for shenas"
requires-python = ">=3.11"
dependencies = [
    "shenas-transformer-core",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project.entry-points."shenas.transformers"]
sql = "shenas_transformers.sql:SqlTransformer"

[tool.hatch.version]
source = "vcs"
tag-pattern = "transformer-sql/v(?P<version>[^+]+)"
raw-options.root = "../../.."
raw-options.git_describe_command = ["git", "describe", "--dirty", "--tags", "--long", "--match", "transformer-sql/v*"]

[tool.hatch.build.targets.wheel]
packages = ["shenas_transformers"]
