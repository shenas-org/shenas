[project]
name = "shenas-source-imf"
dynamic = ["version"]
description = "IMF World Economic Outlook and financial statistics for shenas"
requires-python = ">=3.11"
dependencies = [
    "dlt[duckdb]>=1.24.0",
    "httpx>=0.28.0",
    "shenas-source-core",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project.entry-points."shenas.sources"]
imf = "shenas_sources.imf.source:IMFSource"

[tool.hatch.version]
source = "vcs"
tag-pattern = "source-imf/v(?P<version>[^+]+)"
raw-options.root = "../../.."
raw-options.git_describe_command = ["git", "describe", "--dirty", "--tags", "--long", "--match", "source-imf/v*"]

[tool.hatch.build.targets.wheel]
packages = ["shenas_sources"]
