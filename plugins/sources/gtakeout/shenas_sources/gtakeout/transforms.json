[
  {
    "source_duckdb_schema": "sources",
    "source_duckdb_table": "gtakeout__youtube_watch_history",
    "target_duckdb_schema": "datasets",
    "target_duckdb_table": "events__events",
    "description": "Map YouTube watch history to unified events timeline",
    "sql": "SELECT 'gtakeout' as source, title_url || '_' || time as source_id, time::TIMESTAMP as start_at, NULL as end_at, NULL as duration_min, title, NULL as event_description, NULL as location, CASE WHEN product = 'YouTube Music' THEN 'music' ELSE 'video' END as category, false as all_day, json_object('channel', channel_name, 'channel_url', channel_url, 'video_url', title_url, 'product', product) as event_metadata FROM sources.gtakeout__youtube_watch_history WHERE time IS NOT NULL"
  }
]
