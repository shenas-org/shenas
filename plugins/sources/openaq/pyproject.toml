[project]
name = "shenas-source-openaq"
dynamic = ["version"]
description = "OpenAQ air quality monitoring data for shenas"
requires-python = ">=3.11"
dependencies = [
    "dlt[duckdb]>=1.24.0",
    "httpx>=0.28.0",
    "shenas-source-core",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project.entry-points."shenas.sources"]
openaq = "shenas_sources.openaq.source:OpenAQSource"

[tool.hatch.version]
source = "vcs"
tag-pattern = "source-openaq/v(?P<version>[^+]+)"
raw-options.root = "../../.."
raw-options.git_describe_command = ["git", "describe", "--dirty", "--tags", "--long", "--match", "source-openaq/v*"]

[tool.hatch.build.targets.wheel]
packages = ["shenas_sources"]
