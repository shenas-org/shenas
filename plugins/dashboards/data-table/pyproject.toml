[project]
name = "shenas-dashboard-data-table"
dynamic = ["version"]
description = "Data table with filtering, sorting, pagination, and column resizing"
requires-python = ">=3.11"
dependencies = [
    "shenas-dashboard-core",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project.entry-points."shenas.dashboards"]
data-table = "shenas_dashboards.data_table:DataTableComponent"

[tool.hatch.version]
source = "vcs"
tag-pattern = "dashboard-data-table/v(?P<version>[^+]+)"
raw-options.root = "../../.."
raw-options.git_describe_command = ["git", "describe", "--dirty", "--tags", "--long", "--match", "dashboard-data-table/v*"]

[tool.hatch.build.targets.wheel]
packages = ["shenas_dashboards"]
