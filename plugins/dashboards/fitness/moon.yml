$schema: 'https://moonrepo.dev/schemas/project.json'
id: 'dashboard-fitness'

language: 'typescript'
stack: 'frontend'
tags: ['plugin', 'dashboard']

dependsOn:
  - 'vendor'

tasks:
  js-type-check:
    deps:
      - 'vendor:npm-install'
