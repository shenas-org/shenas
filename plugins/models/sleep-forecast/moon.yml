$schema: 'https://moonrepo.dev/schemas/project.json'
id: 'model-sleep-forecast'

language: 'python'
stack: 'data'
tags: ['model']

dependsOn:
  - 'model-core'

workspace:
  inheritedTasks:
    exclude: ['python-type-check']
