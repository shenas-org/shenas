[project]
name = "shenas-dataset-outcomes"
dynamic = ["version"]
description = "Canonical daily outcomes schema for shenas"
requires-python = ">=3.11"
dependencies = [
    "shenas-dataset-core",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project.entry-points."shenas.datasets"]
outcomes = "shenas_datasets.outcomes:OutcomesSchema"

[tool.hatch.version]
source = "vcs"
tag-pattern = "dataset-outcomes/v(?P<version>[^+]+)"
raw-options.root = "../../.."
raw-options.git_describe_command = ["git", "describe", "--dirty", "--tags", "--long", "--match", "dataset-outcomes/v*"]

[tool.hatch.build.targets.wheel]
packages = ["shenas_datasets"]
