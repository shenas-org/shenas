[project]
name = "shenas-dataset-events"
dynamic = ["version"]
description = "Canonical event metrics for shenas"
requires-python = ">=3.11"
dependencies = [
    "shenas-dataset-core",
    "shenas-plugin-core",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project.entry-points."shenas.datasets"]
events = "shenas_datasets.events:EventsSchema"

[tool.hatch.version]
source = "vcs"
tag-pattern = "dataset-events/v(?P<version>[^+]+)"
raw-options.root = "../../.."
raw-options.git_describe_command = ["git", "describe", "--dirty", "--tags", "--long", "--match", "dataset-events/v*"]

[tool.hatch.build.targets.wheel]
packages = ["shenas_datasets"]
