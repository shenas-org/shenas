[project]
name = "shenas-component-event-gantt"
dynamic = ["version"]
description = "Event timeline Gantt chart visualization for shenas"
requires-python = ">=3.11"
dependencies = [
    "shenas-component-core",
    "shenas-schema-events",
]

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[project.entry-points."shenas.components"]
event-gantt = "shenas_components.event_gantt:EventGanttComponent"

[tool.hatch.version]
source = "vcs"
tag-pattern = "component-event-gantt/v(?P<version>[^+]+)"
raw-options.root = "../../.."
raw-options.git_describe_command = ["git", "describe", "--dirty", "--tags", "--long", "--match", "component-event-gantt/v*"]

[tool.hatch.build.targets.wheel]
packages = ["shenas_components"]
