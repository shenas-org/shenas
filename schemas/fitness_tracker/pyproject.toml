[project]
name = "shenas-schema-fitness-tracker"
dynamic = ["version"]
description = "Canonical fitness tracker metrics schema for shenas"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.entry-points."shenas.schemas"]
fitness-tracker = "shenas_schemas.fitness_tracker:SCHEMA"

[tool.hatch.version]
path = "VERSION"
pattern = "(?P<version>.+)"

[tool.hatch.build.targets.wheel]
packages = ["shenas_schemas"]
